---
all:
  vars:
    ansible_user: ubuntu
    ansible_become: true
    # ansible_ssh_private_key_file: ~/.ssh/sunboys_k3s

  children:
    k3s_server:
      hosts:
        control-plane-1:
          ansible_host: 10.0.0.10
          k3s_node_ip: 10.0.0.10

    k3s_agents:
      hosts:
        worker-1:
          ansible_host: 10.0.0.11
          k3s_node_ip: 10.0.0.11
        worker-2:
          ansible_host: 10.0.0.12
          k3s_node_ip: 10.0.0.12
